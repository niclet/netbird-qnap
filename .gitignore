# Upstream netbird source (cloned at build time)
src/

# Compiled binaries
netbird
qpkg/x86_64/netbird

# QPKG build output
*.qpkg
qpkg/build/
qpkg/QDK

# Committed brand icons (netbird_80.png, netbird_100.png) are tracked so the
# App Center repo.xml icon URLs resolve on the main branch. CI regenerates them
# from the upstream logo at build time but does not commit them. The grayscale
# disabled-state icon is CI-only.
qpkg/icons/netbird_gray.png

# OS artifacts
.DS_Store
Thumbs.db
